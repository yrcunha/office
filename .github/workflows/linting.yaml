name: Linting

on: pull_request

jobs:
  prettier:
    name: Prettier
    uses: setup.yml
    steps:
      - run: npm run lint:prettier:check

  eslint:
    name: Eslint
    uses: setup.yml
    steps:
      - run: npm run lint:eslint:check
